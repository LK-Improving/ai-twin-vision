{
  "name": "@dt/data-processor",
  "version": "1.0.0",
  "description": "企业级数字孪生低代码平台 — 数据处理器与安全脚本沙箱（Worker realm + 能力白名单）",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "echo \"consumed as source via vite alias\"",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src --ext .ts",
    "test": "vitest run"
  },
  "dependencies": {
    "@dt/shared-types": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "vitest": "^2.0.5"
  },
  "license": "MIT"
}
