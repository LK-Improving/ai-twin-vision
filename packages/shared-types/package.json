{
  "name": "@dt/shared-types",
  "version": "0.1.0",
  "private": true,
  "description": "前后端共享的接口契约：统一响应体、错误码、DTO 与业务枚举",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json --watch",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "clean": "rimraf dist"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}
