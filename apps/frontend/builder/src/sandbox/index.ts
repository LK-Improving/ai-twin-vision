/**
 * 兼容出口：脚本沙箱已上提为 @dt/data-processor（迭代 1.2 中台能力包化）。
 *
 * 保留本文件只为不改动既有 import 路径（event-runtime / data-runtime /
 * DataBindingPanel / ScriptEditorDialog / PreviewView 都从 '@/sandbox' 取）；
 * 新代码请直接依赖 '@dt/data-processor'。
 */
export * from '@dt/data-processor';
